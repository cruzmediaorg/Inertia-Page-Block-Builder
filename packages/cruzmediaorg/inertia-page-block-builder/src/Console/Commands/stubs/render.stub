<template>
  <div>
    <h1>{{ title }}</h1>
    <p> {{ content }} </p>
  </div>
</template>

<script setup>
import { computed } from 'vue';

// Props
const props = defineProps({
  title: {
    type: String,
    required: true,
  },
  content: {
    type: String,
    default: '',
  },
});

const blockName = computed(() => '{{ name }}'.toLowerCase());
</script>
